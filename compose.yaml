services:
  helios:
    build:
      context: .
    image: project-helios:local
    ports:
      - "3000:3000"
    environment:
      SITE_URL: "${SITE_URL:-http://localhost:3000}"
    restart: unless-stopped
